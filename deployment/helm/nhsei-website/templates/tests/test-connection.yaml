apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nhsei-website.fullname" . }}-test-connection"
  labels:
    {{- include "nhsei-website.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nhsei-website.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
