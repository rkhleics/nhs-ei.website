apiVersion: v1
kind: Service
metadata:
  name: {{ include "nhsei-website.fullname" . }}
  labels:
    {{- include "nhsei-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "nhsei-website.selectorLabels" . | nindent 4 }}
